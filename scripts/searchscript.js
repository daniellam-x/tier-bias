function search(){
    /*
    // Stores posts in order of most relevant
    const searchPosts = [];
    var postTitle = "";
    var postSummary = "";
    
    // Search for key words in post, those that uses keywords most is higher up on list
    for (let i = 0; i < posts.length; i++) {
        postTitle = posts[i][0];
        postSummary = posts[i][1];
        
        // Checks each word in title
        
        
        // Checks each word in summary
        
    }
    
    */
}